import { SafehtmlpipePipe } from './safehtmlpipe.pipe';

describe('SafehtmlpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SafehtmlpipePipe();
    expect(pipe).toBeTruthy();
  });
});
